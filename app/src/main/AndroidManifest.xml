<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.haoyd.learn">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".common.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".functions.handler.HandlerActivity"></activity>
        <activity android:name=".functions.fragmentlifecycle.LifecycleFragmentActivity" />
        <activity
            android:name=".functions.activitylifecycle.LifecycleDialogActivity"
            android:theme="@style/dialogActivity" />
        <activity android:name=".functions.activitylifecycle.Lifecycle2Activity" />
        <activity android:name=".functions.activitylifecycle.Lifecycle1Activity" />
        <activity
            android:name=".common.MsgShowActivity"
            android:launchMode="singleTop" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>